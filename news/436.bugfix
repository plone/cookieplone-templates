Generate `dependabot.yml` under `.github`, where GitHub reads it from, instead of the root of the codebase, where it was ignored. The file now ships with every template under `templates/ci`, so every codebase with a CI configuration gets a working Dependabot configuration. @ericof
